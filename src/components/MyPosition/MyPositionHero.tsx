import { CrowdfundExperience } from '../../pages/CrowdfundExperience'

/** Hero-layout My Position (bottom corners + full-screen graph). */
export function MyPositionHero() {
  return <CrowdfundExperience initialView="myposition" />
}
